{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://streamline.com/newStatusSchema",
	"name": "New Status Schema",
	"description": "A schema for creating a new status in the streamline application",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"description": "The name of the status"
		},
		"userId": {
			"type": "string",
			"description": "The id of the user creating the new status"
		}
    },
    "required": ["name", "userId"]
}