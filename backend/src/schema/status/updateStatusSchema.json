{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://streamline.com/updateStatusSchema",
	"name": "Update Status Schema",
	"description": "A schema for updating an existing status in the streamline application",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"description": "The name of the status"
		}
	},
	"required": ["name"]
}
