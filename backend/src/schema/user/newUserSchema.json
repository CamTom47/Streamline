{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://streamline.com/newUserSchema",
    "name": "New User Schema",
    "description": "A schema for new users in the streamline application",
    "type": "object",
    "properties": {
        "firstName": {
            "type": "string",
            "description": "The title of the task"
        },
        "lastName": {
            "type": "integer",
            "description": "The id of the user creating the task"
        },
        "email": {
            "type": "integer",
            "description": "The id of the category the task belongs to"
        },
        "username": {
            "type": "string",
            "description": "The date the task is due to be completed"
        },
        "password": {
            "type": "integer",
            "description": "The priority id of the task"
        },
        "role": {
            "type": "integer",
            "description": "The id of the list the task is on"
        }
    },
    "required": ["firstName", "lastName", "username", "password", "role"]

}