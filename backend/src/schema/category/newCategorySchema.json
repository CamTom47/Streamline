{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://streamline.com/newCategorySchema",
	"name": "New Category Schema",
	"description": "A schema for new categories in the streamline application",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"description": "The title of the category"
		},
        "userId": {
            "type": "integer",
            "description": "The id of the user creating the new category"
        }
	},
	"required": ["name", "userId"]
}
